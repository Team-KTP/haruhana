# CHANGELOG

> 변경 이력은 PR 번호 기준으로 묶어서 기록합니다.
> 각 버전은 `docs/` 문서 구조 개편 시점을 기준으로 구분합니다.

---

## [v2] 2026-04-07 — 다중 카테고리 학습 · AI 피드백 · 안정성 개선

> 기준 커밋: `07785c5` (현재 HEAD)
> 이전 기준: `22ffba9` (PR #165 문서 구조 개편)

### 기능 추가

- **다중 학습 설정 지원** (#197, #198, #199)
  - 회원이 학습 카테고리를 최대 5개까지 등록 가능
  - 동일 카테고리 토픽 + 난이도 조합 중복 불가
  - 프로필 조회 응답이 단일 `preference` → `memberPreferences` 리스트로 변경
  - 설정 수정 시 특정 `preferenceId`를 지정하여 해당 설정만 변경 가능
  - API: `POST /v1/members/preferences`, `PATCH /v1/members/preferences/{preferenceId}`

- **하루 다중 문제 제공** (#215)
  - 학습 설정 수만큼 하루에 여러 문제를 받을 수 있도록 변경
  - `GET /v1/daily-problem/today`, `GET /v1/daily-problem?date=` 응답이 List 형태로 변경

- **AI 답안 피드백 (채점) 기능** (#201)
  - 풀이 제출 시 AI가 자동으로 채점 (비동기 처리)
  - 채점 등급: `EXCELLENT` / `GOOD` / `FAIR` / `POOR`
  - 채점 항목: 잘한 점, 부족한 점, 개선 방향
  - `SUBMISSION_FEEDBACK` 엔티티 신규 추가
  - API: `GET /v1/submissions/{submissionId}/feedbacks`
  - V3 프롬프트 도입으로 문제 생성 품질 개선 (면접 관점 심화 개념 포함)

- **오전 학습 독려 알림** (#200)
  - 매일 오전 08:30 전체 회원에게 푸시 알림 발송
  - 알림 유형: `DAILY_STUDY_REMINDER` 추가

- **회원 탈퇴 시 익명화** (#170)
  - 탈퇴 시 `login_id`와 `nickname`을 `deleted_{id}_{uuid8}` 형식으로 즉시 익명화
  - 탈퇴 후 동일 ID/닉네임으로 재가입 가능하도록 unique 제약 충돌 방지

- **신규 문제 생성 시 캐시 즉시 무효화** (#214)
  - 회원 첫 문제 생성 시 해당 회원의 오늘 문제 캐시를 즉시 무효화
  - 새로 생성된 문제가 클라이언트에 즉시 반영되도록 개선

- **최근 출제 문제 중복 방지** (#190)
  - 문제 생성 프롬프트에 최근 출제된 문제 제목을 포함하여 중복 생성 방지

- **FCM 알림 발송 실패 시 Slack 오류 알림** (#189)

### 개선 및 수정

- `PROBLEM.ai_answer` 필드 타입을 `VARCHAR(5000)` → `TEXT`로 변경 (#208, #211)
  - 길이 제한 제거로 긴 AI 답변 저장 가능
- `SUBMISSION_FEEDBACK.strengths/weaknesses/suggestion` 필드 타입을 `TEXT`로 통일 (#211)
- 문제 생성 프롬프트 V2 → V3으로 업그레이드 (#172, #201)
- 스케줄러 예외 발생 시 Slack 오류 알림 전송 (#183)
- 문제 생성 및 백업 할당 실패 시 오류 알림 전송 (#209)

### 버그 수정

- JWT 인증 실패 시 EntryPoint 이중 호출 문제 해결 (#177)
- 카테고리 삭제 후 동일 이름으로 재생성 시 오류 해결 (#179)
- 미풀이 알림 발송 시 회원 디바이스 조회 로직 개선 (#180)
- Prometheus 스크래핑 요청에 JwtFilter 적용되어 WARN 로그 발생하던 문제 해결 (#185)
- nginx `conf.d` 마운트 경로 오류로 컨테이너 기동 실패하던 문제 해결 (#186)
- JwtFilter 경로 매칭 로직 개선 (#205)

---

## [v1] 2025-xx-xx — 문서 구조 개편 및 내용 고도화

> 기준 커밋: `22ffba9` (PR #165)

### 주요 기능 (이 시점까지 구현된 기능)

- 회원 관리 및 JWT 인증
- 오늘의 문제 설정 (단일 카테고리) 및 AI 문제 생성 (Gemini)
- 문제 풀이 제출 및 AI 채점 (비동기)
- 스트릭 시스템 (낙관적 락)
- FCM 푸시 알림 (문제 생성 / 스트릭 임박 / 미풀이 독려)
- 프로필 이미지 업로드 (S3 Presigned URL)
- 관리자 기능 (회원 관리, 카테고리 관리, 문제 목록 조회, 통계 대시보드)
- 모니터링 (Prometheus + Grafana + Sentry + Slack)
- Soft Delete (논리 삭제) 전략
